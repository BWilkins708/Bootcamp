class Scooter{

    //Properties
    type;
    percentage;
    scooter_id;

    constructor(type, percentage, scooter_id) {
        this.type = type;
        this.percentage = percentage;
        this.scooter_id = scooter_id;
    }

    isAvailable(scooter_id) {
        new date();
        date.getdate

        if (this.percentage < 40){
            return ("This scooter is out of use")
        }

        console.log("Start ride at " + date)
        return scooter_id;
    }
}

module.exports = Scooter